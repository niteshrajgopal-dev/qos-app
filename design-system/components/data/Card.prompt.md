# Card

Standard content surface: 12px radius, hairline border, low shadow.

```jsx
<Card header="Integration health" actions={<Button size="sm" variant="secondary">View all</Button>}>…</Card>
```

`padding="none"` when the card holds a table. `interactive` adds hover elevation for whole-card links.
